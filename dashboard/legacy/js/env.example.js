/*
 * Local-dev config for the Supabase client. Copy this file to env.js
 * (gitignored) and fill in your project's URL + anon key — both are
 * browser-safe values from Supabase Project Settings → API.
 *
 * In production (Vercel), env.js is generated at build time by
 * scripts/build-env.mjs from SUPABASE_URL / SUPABASE_ANON_KEY project
 * environment variables — you never edit the deployed file by hand.
 */
window.__INNOV8_ENV__ = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-public-key"
};
