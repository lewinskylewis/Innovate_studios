# Non-website production/source files (not part of the deployed site)
/tmp/
/NIKE COMMERCIAL/
/SOCIAl MEDIA/
/Hero Section/
/Untitled Project.aep
/.agents/

# Studio Dashboard — generated locally by copying env.example.js, or by
# dashboard/build-env.mjs during a Vercel build. Never contains a secret
# (see .env.example), but it's environment-specific and shouldn't be
# committed.
/dashboard/public/env.js

# Studio Dashboard — Vite + React app (dashboard/package.json)
/dashboard/node_modules/
/dashboard/dist/

# Local environment files
.env
.env.local

# Supabase CLI local cache
/supabase/.temp/
